package objectOriented;

public class Operator {
/**
 * 算数运算符:
 * + - * / % ++ --
 * 赋值运算符:
 * = += -= *= /= %=
 * 关系运算符:
 * > < >= <= == != 
 * 逻辑运算符:
 * && || ! 
 * 位运算符:
 * & | ^ ~ << >> >>> 
 * 三元运算符:
 * ? :
 * instanceof 运算符:
 * String name = "James"; boolean result = name instanceof String; 
 * 由于 name 是 String 类型，所以返回真
 */
/**
 * 运算符的优先级:括号点,单目,双目{算术,移位,关系,按位与异或或,逻辑与或},三目{条件},赋值,逗号
 * 类别			操作符			关联性
 * 后缀			() [] . 		左到右
 * 一元			+ - ！〜	++ --	从右到左
 * 乘性 			* /％			左到右
 * 加性 			+ -				左到右
 * 移位 			>> >>>  << 		左到右
 * 关系 			>> = << = 		左到右
 * 相等 			==  !=			左到右
 * 按位与		＆				左到右
 * 按位异或		^				左到右
 * 按位或		|				左到右
 * 逻辑与		&&				左到右
 * 逻辑或		||				左到右
 * 条件			？：				从右到左
 * 赋值			= + = - = * = / =％= >> = << =＆= ^ = | =			从右到左
 * 逗号			,				左到右
 */
	public static void main(String[] args) {
		// TODO Auto-generated method stub

	}

}
